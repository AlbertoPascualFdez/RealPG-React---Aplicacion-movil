/**
 * @format
 */

import {AppRegistry} from 'react-native';

import {name as appName} from './app.json';
import { RealPgApp } from './src/RealPGApp';

AppRegistry.registerComponent(appName, () => RealPgApp);
